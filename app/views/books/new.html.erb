<h1>新規投稿フォーム</h1>
<%= form_with model: @book, local:true do |f| %>
  <h4>Title</h4>
  <%= f.text_field :shop_name %>
  <h4>Opinion</h4>
  <%= f.text_area :caption %>
  <%= f.submit 'Create' %>
<% end %>